# Important Questions – Root

1. What UID does the root user have, and why is it special?
2. What is the difference between `su` and `sudo`?
3. How do you allow a specific user to run only one command with `sudo`?
4. What file controls `sudo` permissions, and how should it be edited?
5. What are Linux capabilities, and how do they differ from full root access?
6. How would you give a binary the ability to bind to port 80 without running it as root?
7. What is a `chroot` jail, and what is one of its limitations as a security measure?
8. Where are successful and failed `sudo` attempts logged?
9. How do you disable direct root login over SSH?
10. What is the principle of least privilege and why does it matter?
