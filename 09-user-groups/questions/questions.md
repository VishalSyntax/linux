# Important Questions – User & Groups

1. What are the seven colon-separated fields in `/etc/passwd`?
2. Why are password hashes stored in `/etc/shadow` instead of `/etc/passwd`?
3. How do you create a new user with a home directory and default shell?
4. What is the difference between `useradd` and `adduser`?
5. How do you add an existing user to a supplementary group?
6. What command shows all groups a user belongs to?
7. How do you lock and unlock a user account?
8. What does `chage -l <username>` display?
9. What is the primary group vs supplementary group of a user?
10. How does PAM (Pluggable Authentication Modules) fit into Linux authentication?
