# Important Questions – Kernel

1. What are the four main responsibilities of the Linux kernel?
2. What command shows the currently running kernel version?
3. What is a kernel module, and why are modules useful?
4. How do you list all currently loaded kernel modules?
5. What is the difference between `insmod` and `modprobe`?
6. How do you read or set a kernel parameter at runtime?
7. What information does `dmesg` display, and when is it useful?
8. What does "monolithic kernel" mean, and is Linux monolithic?
9. Where are kernel parameters stored persistently across reboots?
10. What is the purpose of `/proc/sys`?
