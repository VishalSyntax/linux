# Important Questions – Shell Scripting

1. What is a shebang line, and why is it needed?
2. What is the difference between `$@` and `$*` inside a script?
3. How do you check the exit status of the last command?
4. Write a one-liner that prints numbers 1 to 10.
5. What does `set -e` do at the top of a script?
6. How do you define and call a function in bash?
7. What is the difference between single quotes and double quotes in bash?
8. How do you schedule a script to run every day at 2 AM using cron?
9. What is the purpose of `trap 'cleanup' EXIT`?
10. How do you read user input and validate that it is a number?
