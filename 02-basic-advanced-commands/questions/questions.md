# Important Questions – Basic & Advanced Commands

1. What is the difference between an absolute path and a relative path?
2. How do you list hidden files with `ls`? What makes a file "hidden" in Linux?
3. What is the difference between `>` and `>>` for redirection?
4. How would you search all `.log` files under `/var` for the word "error" (case-insensitive)?
5. Explain the difference between `find` and `locate`.
6. What does `2>&1` mean in a shell command?
7. How do you create a compressed archive with `tar`? How do you extract one?
8. What does `awk '{print $2}' file.txt` do?
9. How do you count the number of lines in a file?
10. What is the difference between `kill`, `kill -9`, and `pkill`?
