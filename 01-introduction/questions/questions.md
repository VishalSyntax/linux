# Important Questions – Introduction

1. What is the Linux kernel, and what role does it play in the OS?
2. Name five popular Linux distributions and explain how they differ.
3. What is the difference between the kernel space and user space?
4. What does the file `/etc/os-release` contain?
5. What is the purpose of the `man` command? How do you search inside a man page?
6. Describe the Linux boot process from BIOS/UEFI to the login prompt.
7. What does `uname -a` show?
8. What is an open-source licence, and which licence does the Linux kernel use?
